app.controller("threadIndexCtrl", ["$scope", "$firebaseArray",
    function($scope, $firebaseArray, $log) {
//        var ref = new Firebase("https://djenchat.firebaseio.com/");
//        $scope.messages = $firebaseArray(ref);
//        console.log($scope.messages);
    }
]);